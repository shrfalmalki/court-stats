# 🚀 كيفية تشغيل الموقع بالكامل (الخام + قاعدة البيانات)

بما أن موقعك يستخدم **Node.js** و **SQLite** (قاعدة بيانات)، فإن GitHub Pages لا يكفي لأنه مخصص للصفحات الثابتة فقط.

لجعل الموقع يعمل بالكامل (تسجيل الدخول، حفظ البيانات)، تحتاج لرفعه على استضافة تدعم Node.js.
أفضل خيار مجاني وسهل هو **Render.com**.

## خطوات الرفع على Render

1. **التسجيل:**
   - اذهب إلى [https://render.com](https://render.com).
   - سجل الدخول باستخدام حسابك في **GitHub**.

2. **إنشاء خدمة جديدة:**
   - اضغط على زر **New +** واختر **Web Service**.
   - اختر المستودع الخاص بك (`court-stats`) من القائمة واضغط **Connect**.

3. **الإعدادات:**
   - **Name:** اختر اسماً للموقع (مثلاً `court-system`).
   - **Region:** اختر الأقرب (مثلاً Frankfurt).
   - **Branch:** تأكد أنه `main`.
   - **Runtime:** اختر `Node`.
   - **Build Command:** `npm install`
   - **Start Command:** `node server.js`
   - **Instance Type:** اختر `Free` (مجاني).

4. **تشغيل الموقع:**
   - اضغط على **Create Web Service**.
   - انتظر قليلاً حتى يتم البناء (Build) والتشغيل.
   - سيظهر لك رابط في الأعلى (مثلاً `https://court-system.onrender.com`).
   - هذا هو الرابط الذي سيعمل عليه النظام بالكامل!

---

## ⚠️ ملاحظة هامة جداً بخصوص قاعدة البيانات (SQLite)

في الخطة المجانية من Render (وبمعظم الاستضافات السحابية المؤقتة):
*   **ملف قاعدة البيانات (`beneficiaries.db`) قد يتم مسحه** عند إعادة تشغيل الخادم أو رفع تحديث جديد.
*   هذا يعني أن البيانات التي يدخلها المستخدمون قد تضيع إذا تم إعادة تشغيل الخادم.

**الحل للعمل الدائم:**
1.  استخدام **Render Disk** (خيار مدفوع) لربط قرص تخزين يحفظ ملف قاعدة البيانات.
2.  أو استخدام خدمة مثل **Glitch.com** (سهلة جداً وتحفظ ملفات SQLite تلقائياً).

### خيار بديل سريع (Glitch):
1. اذهب لـ [Glitch.com](https://glitch.com).
2. اختر **New Project** -> **Import from GitHub**.
3. الصق رابط مستودعك.
4. سيعمل الموقع فوراً وتحفظ البيانات بشكل دائم (مجاناً للمشاريع الصغيرة).
